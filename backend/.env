# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=dunhill18
DB_NAME=profileapp
DB_SSLMODE=disable

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_to_random_letters
JWT_EXPIRY_HOURS=24

# Environment
ENV=development

# Gmail SMTP Configuration for OTP emails
# Step 1: Go to Google Account → Security → 2-Step Verification (enable it)
# Step 2: Go to Google Account → Security → App Passwords
# Step 3: Create app password for "Mail" → "Other" → name it "ProfileApp"
# Step 4: Copy the 16-character password and paste below (spaces are okay)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dunhillpallen49@gmail.com
SMTP_PASS=rfag afeh gbhx hrya
